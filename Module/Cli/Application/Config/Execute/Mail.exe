{ if($var && is.empty(is.empty(true))==1) }
	true
	{if ("yes" == "yes")}
		design
	{elseif ("no" == "no")}
		asshole
	{elseif("no1" == "no1")}
		walla
	{/if}
{ elseif(false === true) }
 yes
{ elseif(false === false) }
	{ is.empty(null) }
	false
	blah {if ("pietje{$priya.parse.read.url}" === "pietje{$priya.parse.read.url}") }
nicer2{/if}

{ elseif(true ===  is.empty(null)) }

	nice3


{ else }
	else
{ / if }
wat is dit dan
/**

token till foreach, while, can echo & unset

foreach ()
	xyz
	foreach()
		abc
		terminal.readline('def');



**/