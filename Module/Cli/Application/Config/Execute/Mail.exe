{ if($var && is.empty(is.empty(true))==1) }
	true
	{if ("yes" == "yes")}
		design
	{elseif ("no" == "no")}
		asshole
	{elseif("no1" == "no1")}
		walla
	{/if}
{ elseif(false === true) }
 yes
{ elseif(false === false) }
	{ is.empty(null) }
	false
	{if ('pietje' == 'pietje') }
nice2
	{/if}

{ elseif(true ===  is.empty(null)) }

	nice


{ else }
	else
{ / if }
wat is dit dan