	what is
	{$test = 1 + 2 - 1 + 1 +3}
	{while(is.empty($mail.username))}
		{$test = 1 + 2 - (1 + 1) +3}
		{$mail.username = terminal.readline('Username: {$counter}:')}
		{$counter  = $counter+1}
huh: {$counter}
		{if ($counter > 3)}
			{$test14 = true}
			{break()}
		{/if}
		wat {$test14}
		{$end.while = true}
	{/while}
	wat {$test14}