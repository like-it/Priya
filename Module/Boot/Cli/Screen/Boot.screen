{$x= 80}
{$y = 50}
{$x += 1}

{$cherry = $x}

{$z = $x}
{$z = $x}

{terminal.start()}
{terminal.grid(0, 0, 'auto', 'auto', 10, 5)}
{terminal.screen.goto(0, 0)}
{terminal.write.line("Welcome to Priya version {$priya.version}")}
{terminal.screen()}
{terminal.screen.goto(10, 10)}
{$command = terminal.read.line('Boot: ')}