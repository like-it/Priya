{
"@see" : "http://priya.software/Documentation/Javascript",
"name" : "{$url|basename:'.json'}",
"core" : "{$web.root}Priya/Public/Js/Prototype/Core/",
"prototype" : "{$core|dir.name}",
"extension" : ".prototype.js",
"collect" : {
    "web" : "{object.merge($web,{prototype: $prototype, core: $core})}",
    "expose" : {
        "namespace" : "_",
        "require" : "require",
        "window" : {
            "isset" : "isset",
            "empty" : "empty",
            "microtime" : "microtime",
            "trim" : "trim",
            "run" : "run",
            "select" : "select",
            "get" : "get",
            "request" : "request",
            "attach" : "attach"
        }
    }
},
"require" : {
    "core" : [
        "{$core}Active{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Append{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Attach{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Attribute{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Basename{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Calculate{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Children{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Class{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Clone{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Closest{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Collection{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Content{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Create{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Create.selector{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Css{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Data{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Debug{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Empty{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Exception{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Explode.multi{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Explode{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Extended{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Find{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Function.exists{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Html{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Implode{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}In.array{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Is.array{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Is.nodelist{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Is.numeric{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Isset{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Jid{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Link{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Loader{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Methods{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Microtime{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Namespace{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Compare.natural{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Next{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Object.delete{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Object.get{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Object.horizontal{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Object.merge{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Object.set{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Off{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}On{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Previous{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Rand{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Refresh{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Remove{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Request{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Round{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Run{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Script{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Scrollbar{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Select{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Str.replace{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Str.stri{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Style{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Trigger{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Trim{$extension}?{$priya.revision | default : $priya.version}",
        "{$core}Val{$extension}?{$priya.revision | default : $priya.version}"
    ],
    "file" : [
        "{$prototype}Usleep{$extension}?{$priya.revision | default : $priya.version}"
    ]
}
}